$(document).ready(function () {
		initTeam();
});

var initTeam = function () {
		const swiperTeam = new Swiper('.slider-team', {
				slidesPerView: 'auto',
				freeMode: true,
				speed: 200,
		});
};
